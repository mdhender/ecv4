*.tgz

# editor and agent files
.amp/
.claude/
.idea/
next-prompt*.md

# Local build products
/bin/
/dist/
/earl
/game-server
*.out
*.test
/tmp/

# Environment files (see internal/dotenv). Only the *.local files may hold
# secrets and are ignored; the shared .env and .env.<env> files are committed.
.env.local
.env.*.local
*.local
